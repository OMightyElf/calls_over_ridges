<div class="panel-header">
	<h4>熱門</h4>
</div>
<div class="panel-body">
	<% Tag.first(3).each_with_index do |t, i| %>	
		<%= render partial: "partials/small_post", collection: t.posts.take(perpage) %>
	<% end %>
</div>