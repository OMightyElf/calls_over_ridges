<h2>請確認匯入資料欄位是否相符（藍色為系統欄位，綠色為匯入excel之欄位）：</h2>
<%= link_to '返回', admin_mass_import_children_data_path, class: 'button gray' %>
<%= link_to "確認匯入", admin_mass_import_children_data_confirm_import_path, class: 'button' %>
<br>
<br>
<table>
<tr class="blue">
	<td>孩童編號</td>
	<td>First Name</td>
	<td>Last Name</td>
	<td>性別</td>
	<td>生日</td>
</tr>
<% (@raw_list.first_row..@raw_list.last_row).each do |i| %>
<tr class="<%= 'green' if i==1 %>">
	<% @raw_list.row(i).each do |td| %>
	<td><%= td %></td>
	<% end %>
</tr>
<% end %>

</table>
